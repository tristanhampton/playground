<section class="component component--latest-bookmarks">
  <h2 class="h4">Latest Bookmarks</h2>
  <p>These are links to things I've come accross online that I think are interesting, or deserving of remembering to look at later.</p>
  <ul class="text-list">
    {% for bookmark in d_bookmarks | reverse %}
      {% if loop.index0 < 20 %}
        <li class="bookmark-item">
          <a class="h5" href="{{bookmark.link}}">{{ bookmark.title }}
            <span class="sr-only">(external link)</span></a>
          {% if bookmark.description %}
            {{ bookmark.description|markdown|safe }}
          {% endif %}
        </li>
      {% endif %}
    {% endfor %}
  </ul>
</section>