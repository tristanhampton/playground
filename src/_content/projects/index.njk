---
layout: default
siteTitle: "Tristan Hampton"
title: "Projects"
permalink: "/projects/"
meta_description: "The full list of web projects that I have worked on, along with links to view the real project."
---

<main class="site__main main--projects">

	<section class="banner banner--default">
		<h1>Projects</h1>
		<p>These are websites I've worked on as a Front End Developer. Clicking any of these will take you to the live version of the site.</p>
	</section>

	<section class="component component--standard-wide">
		<h2>Projects with Northern</h2>
		<small><em>January 2024 - Present</em></small>
		<ul class="grid-wrapper grid--extra-large gap--large">
			{% for project in collections.northern | reverse %}
				{% if project.data.published %}
					<li class="project-item">
						<a href="{{ project.data.website_url if project.data.redirect else project.url }}" title="{{ project.data.title }}" target="{{ '_blank' if project.data.redirect else '' }}" rel="noopener noreferrer" class="card size--square">
							<h3>{{ project.data.short }}</h3>
							{% set imgPath = project.page.filePathStem + '.png' %}
							<picture>
								<source srcset="{% getUrl imgPath | resize({ height: 300, width: 300, position: 'top' }) | avif({ quality: 100 }) %}" media="(max-width: 768px)">
								<source srcset="{% getUrl imgPath | resize({ height: 600, width: 600, position: 'top' }) | avif({ quality: 100 }) %}" media="(min-width: 769px)">
								<img src="{% getUrl imgPath | resize({ height: 600, width: 600, position: 'top' }) | avif({ quality: 100 }) %}" alt="screenshot of the points and bezier art piece">
							</picture>
						</a>
					</li>
				{% endif %}
			{% endfor %}
		</ul>

		<hr class="type--baba space--large">

		<h2>Projects with Yellow Pencil</h2>
		<small><em>February 2022 - December 2023</em></small>
		<ul class="grid-wrapper grid--extra-large gap--large">
			{% for project in collections.yellowpencil | reverse %}
				<li class="project-item">
					<a href="{{ project.data.website_url if project.data.redirect else project.url }}" title="{{ project.data.title }}" target="{{ '_blank' if project.data.redirect else '' }}" rel="noopener noreferrer" class="card size--square">
						<h3>{{ project.data.short }}</h3>
						{% set imgPath = project.page.filePathStem + '.png' %}
						<picture>
							<source srcset="{% getUrl imgPath | resize({ height: 300, width: 300, position: 'top' }) | avif({ quality: 100 }) %}" media="(max-width: 768px)">
							<source srcset="{% getUrl imgPath | resize({ height: 600, width: 600, position: 'top' }) | avif({ quality: 100 }) %}" media="(min-width: 769px)">
							<img src="{% getUrl imgPath | resize({ height: 600, width: 600, position: 'top' }) | avif({ quality: 100 }) %}" alt="screenshot of the points and bezier art piece">
						</picture>
					</a>
				</li>
			{% endfor %}
		</ul>

		<hr class="type--trap space--large">

		<h2>Projects with Box Clever</h2>
		<small><em>December 2020 - February 2022</em></small>
		 <ul class="grid-wrapper grid--extra-large gap--large">
			{% for project in collections.boxclever | reverse %}
				<li class="project-item">
					<a href="{{ project.data.website_url if project.data.redirect else project.url }}" title="{{ project.data.title }}" target="{{ '_blank' if project.data.redirect else '' }}" rel="noopener noreferrer" class="card size--square">
						<h3>{{ project.data.short }}</h3>
						{% set imgPath = project.page.filePathStem + '.png' %}
						<picture>
							<source srcset="{% getUrl imgPath | resize({ height: 300, width: 300, position: 'top' }) | avif({ quality: 100 }) %}" media="(max-width: 768px)">
							<source srcset="{% getUrl imgPath | resize({ height: 600, width: 600, position: 'top' }) | avif({ quality: 100 }) %}" media="(min-width: 769px)">
							<img src="{% getUrl imgPath | resize({ height: 600, width: 600, position: 'top' }) | avif({ quality: 100 }) %}" alt="screenshot of the points and bezier art piece">
						</picture>
					</a>
				</li>
			{% endfor %}
		</ul>

		<hr class="type--phone space--large">

		<h2>Other Projects</h2>
		{# <small><em>December 2020 - February 2022</em></small> #}
		 <ul class="grid-wrapper grid--extra-large gap--large">
			{% for project in collections.somethingclever | reverse %}
				<li class="project-item">
					<a href="{{ project.data.website_url if project.data.redirect else project.url }}" title="{{ project.data.title }}" target="{{ '_blank' if project.data.redirect else '' }}" rel="noopener noreferrer" class="card size--square">
						<h3>{{ project.data.short }}</h3>
						{% set imgPath = project.page.filePathStem + '.png' %}
						<picture>
							<source srcset="{% getUrl imgPath | resize({ height: 300, width: 300, position: 'top' }) | avif({ quality: 100 }) %}" media="(max-width: 768px)">
							<source srcset="{% getUrl imgPath | resize({ height: 600, width: 600, position: 'top' }) | avif({ quality: 100 }) %}" media="(min-width: 769px)">
							<img src="{% getUrl imgPath | resize({ height: 600, width: 600, position: 'top' }) | avif({ quality: 100 }) %}" alt="screenshot of the points and bezier art piece">
						</picture>
					</a>
				</li>
			{% endfor %}
		</ul>
	</section>

</main>